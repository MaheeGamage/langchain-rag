{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run app (poetry run python run.py)",
            "type": "debugpy",
            "request": "launch",
            "module": "run",
            "python": "${workspaceFolder}/.venv/bin/python",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
